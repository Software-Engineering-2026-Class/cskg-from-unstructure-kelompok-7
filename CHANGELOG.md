# Changelog

## Final take-home submission

### Added
- Added `docker-compose.yml` as a standard Docker Compose entrypoint that includes the existing `compose.yml` service definition.
- Added `.env.example` documenting required and optional environment variables.
- Added final submission checklist evidence to make repository completeness easier to verify.

### Documented
- Runtime architecture, service ports, and API endpoints in `README.md`.
- Use cases and SPARQL examples in `docs/usecases.md` and `queries/usecases/`.
- Evaluation results, graph statistics, and runtime verification in `docs/evaluation.md` and `docs/evidence/`.

### Fixed
- Addressed final checklist gaps related to Docker naming convention, environment documentation, release notes, and output-coverage evidence.
